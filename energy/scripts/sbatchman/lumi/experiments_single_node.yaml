variables:
  # nodes: [2, 4, 8, 16, 32]
  nodes: [1]
  channels: [2, 4, 8, 16, 32]
  protocols_1: ["Simple", "LL",  "LL128"]
  alg: [Ring, Tree]
  threads: [64, 128, 256, 512]
  partition_lumi: [standard-g]
  base_dir_lumi: ["/users/carpenti/energy-ws/Blink-GPU/energy"]

jobs:
  ####### LUMI ############################################################
   - config: a2a_n{nodes}_default_{partition_lumi}
     command: "srun {base_dir_lumi}/build/bin/a2a_rccl {base_dir_lumi}/logs/a2a_n{nodes}_default_{partition_lumi}.csv"
     cluster_name: "lumi"
     config_jobs:
      - tag: a2a_n{nodes}_default_{partition_lumi}
  
  #  - config: a2a_n{nodes}_algDefault_prot{protocols_1}_ch{channels}_th256_{partition_lumi}
  #    command: "srun {base_dir_lumi}/build/bin/a2a_rccl {base_dir_lumi}/logs/a2a_n{nodes}_algDefault_prot{protocols_1}_ch{channels}_th256_{partition_lumi}.csv"
  #    cluster_name: "lumi"
  #    config_jobs:
  #     - tag: a2a_n{nodes}_algDefault_prot{protocols_1}_ch{channels}_th256_{partition_lumi}
  
  # - config: ar_n{nodes}_alg{alg}_prot{protocols_1}_ch{channels}_th{threads}_{partition_lumi}
  #   command: "srun {base_dir_lumi}/build/bin/ar_rccl {base_dir_lumi}/logs/ar_n{nodes}_alg{alg}_prot{protocols_1}_ch{channels}_th{threads}_{partition_lumi}.csv"
  #   cluster_name: "lumi"
  #   config_jobs:
  #     - tag: ar_n{nodes}_alg{alg}_prot{protocols_1}_ch{channels}_th{threads}_{partition_lumi}
  ########################################################################
